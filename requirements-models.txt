# Model-prerequisites fragment (auto-generated from requirements-full.txt).
#
# Pins ONLY the spaCy and NLTK libraries at the same versions as the repo-wide
# requirements. This lets the image pre-install these two and download the
# spaCy models + NLTK data EARLY in the build (a small, cacheable layer), before
# the long full dependency install. Regenerated by `make update-requirements`.
nltk==3.10.3
spacy==3.8.16
